# Polymarket API Credentials
# Get these from: https://polymarket.com/settings/api-keys
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# IMPORTANT: the fills collector requires these creds to fetch account fills.
# The repo does NOT commit a real .env; create one locally (gitignored):
#   cp .env.example .env
#   $EDITOR .env
#
# To get credentials from 1Password:
#   eval $(op signin)
#   export POLYMARKET_API_KEY=$(op item get "Polymarket API" --field password)
#   export POLYMARKET_API_SECRET=$(op item get "Polymarket API" --field "API Secret")
#   export POLYMARKET_API_PASSPHRASE=$(op item get "Polymarket API" --field "Passphrase")
#
# Or use the helper script:
#   source ./scripts/load-env-from-1password.sh

# Safety: Always dry-run by default. Set to 'false' ONLY when ready to trade live.
POLYMARKET_DRY_RUN=true
